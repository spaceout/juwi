%h1 Listing #{Torrent.count} Torrents
%table.table.table-hover
  %thead
    %tr
      %th Name
      %th Status
      %th Size
      %th Percent
      %th Completed
      %th Rename Status
      %th

  %tbody
    - @torrents.each do |torrent|
      %tr
        %td= link_to torrent.name, torrent
        %td= torrent.status_to_s
        %td= Filesize.from("#{torrent.size} B").pretty
        %td= torrent.percent
        %td= torrent.completed
        %td= torrent.rename_status
        %td= link_to 'Files', torrent_tfiles_path(torrent)
        %td= button_to 'Destroy', torrent, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => "btn btn-primary btn-sm"
