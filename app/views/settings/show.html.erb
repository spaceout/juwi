<h3> <%= "#{@setting.name}" %></h3>

<table>
  <tr>
    <th>Setting</th>
    <th>value</th>
    <th>notes</th>
  </tr>
  <tr>
    <td><%= @setting.name%></td>
    <td><%= @setting.value %></td>
    <td><%= @setting.notes %></td>
  </tr>
</table>
<%= button_to("delete", @setting, method: :delete, data: {confirm: "Are you sure?"}) %>
