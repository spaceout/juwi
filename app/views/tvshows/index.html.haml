%h3= "Listing #{@tvshows.count} TV shows"

%table.table.table-hover.table-condensed
  %thead
    %tr
      %th= "Show Name"
      %th= "Airing Status"
      %th= "Next Airing"
  %tbody
    - @tvshows.each do |tvshow|
      %tr
        %td= link_to(tvshow.title, tvshow)
        %td= tvshow.status
        %td= if tvshow.next_episode_date == nil then "TBD" else "#{tvshow.next_episode_date}" end
