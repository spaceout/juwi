%h3= "Listing #{@tvshows.count} TV shows"

%table.table.table-hover.table-condensed
  %thead
    %tr
      %th= "Show Name"
      %th= "Airing Status"
      %th= "Next Airing"
  %tbody
    - @tvshows.each do |tvshow|
      %tr
        %td= link_to(tvshow.ttdb_show_title, tvshow)
        %td= tvshow.tvr_show_status
        %td= if tvshow.tvr_next_episode_date == nil then "TBD" else "#{tvshow.tvr_next_episode_date}" end
